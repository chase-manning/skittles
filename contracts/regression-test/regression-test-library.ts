export const returnFourty = (): number => {
  return 40;
};

export function returnFifty(): number {
  return 50;
}

export const functionWithParams = (a: number, b: number): number => {
  return a + b;
};
