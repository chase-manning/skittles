import { SkittlesMethod } from "../types/skittles-contract";
declare const addMethodFunction: (yul: string[], method: SkittlesMethod, external?: boolean) => string[];
export default addMethodFunction;
